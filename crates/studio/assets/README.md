# Assets

Place demo GIF here after recording:

```bash
# Record with peek (Linux)
peek

# Or with asciinema + svg-term
asciinema rec demo.cast
svg-term --in demo.cast --out demo.gif
```

## Recommended demo flow (15 seconds)

1. Open `main.rs`
2. Cursor on empty line
3. Type in chat: "Add error handling"
4. Ghost cursor types code (violet)
5. Press Tab → code accepted
6. Done ✨
